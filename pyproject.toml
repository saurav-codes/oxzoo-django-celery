[project]
name = "oxzoo-django-celery"
version = "0.1.0"
description = "ox deploy example: Django API + Celery worker + beat on one VPS"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "celery[redis]==5.6.3",
    "dj-database-url==3.1.2",
    "django==5.2.17",
    "flower==2.0.1",
    "gunicorn==26.2.0",
    "psycopg[binary]==3.3.5",
    "redis==6.4.0",
]
